import memory;

namespace stdlib {

    // A non-mutable "view" into a string's data. The data itself is not owned by this struct.
    public type StringView = struct {
        data: &u8,
        length: u64,
    };

}
