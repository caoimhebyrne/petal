// Using `+`, `-`, `/` and `*`, you can perform addition, subtraction, division, and
// multiplication on values.
func main() -> i32 {
    // Addition
    i32 a = 4 + 5;

    // Subtraction
    i32 b = a / 2;

    // Multiplication
    i32 c = b * 5;

    // Division
    return c / 2;    
}
