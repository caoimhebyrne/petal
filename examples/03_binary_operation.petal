func main() -> i32 {
    // You can perform 'binary operations' on values using the '+' (plus), '-' (minus), '*' (multiply) and '/' (divide)
    // operators.
    i32 a = 5 + 10;  // 15
    i32 b = a - 3;   // 12
    i32 c = b * 2;   // 24
    i32 d = c / 4;   // 6

    return d;
}
