// You can create your own alias for a type using a type declaration.
type MyAwesomeInteger = i32;

func main() -> i32 {
    // MyAwesomeInteger is just an alias for `i32`. It can be used interchangeably.
    MyAwesomeInteger value = 5;
    return value;
}
