// This is a function declaration.
// The function's name is `main`, and it has a return type of `i32`.
//
// NOTE: Specifying the return type is not neccessary in the case of `void`.
func main() -> i32 {
    i32 hello = 200;

    // Any expression can be returned from a function using the `return` keyword.
    return hello;
}