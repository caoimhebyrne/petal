// Functions are only accessible within their own module by default. To expose a function to other module, you must
// use the `public` access modifier.
//
// This module is imported by `main.petal`, and `get_magic_number` is called by it.
public func get_magic_number() -> i32 {
    return 7;
}
