// `if` statements can be used to run code when a certain expression evaluates to be true.
//
// This code should evaluate to have an exit code of 1, try changing the `2` to a `1` to make both
// sides equal on the condition!
func main() -> i32 {
    if 1 == 2 {
        return 0;
    }

    return 1;
}
