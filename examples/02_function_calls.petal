// A function can take inputs in the form of parameters.
func adder(a: i32, b: i32) -> i32 {
    return a + b;
}

func main() -> i32 {
    // Values can be passed to a function in the form of arguments.
    return adder(4, 5);
}
