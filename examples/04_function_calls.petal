// The main function is not the only function. You can have as many functions as you like!
func my_magic_value() -> i32 {
    return 42;
}

func main() -> i32 {
    // You can call on a function by its name. If a function has parameters, you can pass values (arguments) for them
    // between the parenthesis.
    //
    // If a function returns a value, the result of the function call will be that value.
    return my_magic_value();
}
